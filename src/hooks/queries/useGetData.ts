/*

import { useQuery } from '@tanstack/react-query';

// example code
const useGetData = () => {
  return useQuery({
    queryKey: [''],
    queryFn: async () => {
      const { data } = await fetchSomeData();

      return data;
    },
  });
};

export { useGetData };

*/

export {};
